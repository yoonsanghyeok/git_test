<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
<!--   <environments default="development">
    <environment id="development">
      <transactionManager type="JDBC"/>
      <dataSource type="POOLED">
        <property name="driver" value="${driver}"/>
        <property name="url" value="${url}"/>
        <property name="username" value="${username}"/>
        <property name="password" value="${password}"/>
      </dataSource>
    </environment>
  </environments> -->
  
  <typeAliases>
	<typeAlias type="sesoc.global.test.vo.Customer" alias="Customer"/>
	<typeAlias type="sesoc.global.test.vo.Board" alias="Board"/>
	<typeAlias type="sesoc.global.test.vo.Notice" alias="Notice"/>
  </typeAliases>
  
  <mappers>
    <mapper resource="mapper/customerMapper.xml"/>
    <mapper resource="mapper/boardMapper.xml"/>
    <mapper resource="mapper/noticeMapper.xml"/>
  </mappers>

</configuration>